# Runtime

## Purpose

The Runtime executes prompts using an LLM provider.

Runtime contains no business knowledge.

Runtime contains no orchestration logic.

Runtime only manages communication with the selected provider.
